# 🤖 إعداد الشات بوت الذكي - مُوجَز+

## الخطوات المطلوبة:

### 1️⃣ الحصول على Google Gemini API Key

1. اذهب إلى: https://makersuite.google.com/app/apikey
2. سجّل الدخول بحساب Google
3. اضغط على **"Create API Key"**
4. انسخ المفتاح

---

### 2️⃣ إنشاء ملف `.env`

في جذر المشروع، أنشئ ملف اسمه `.env` وضع فيه:

```
GEMINI_API_KEY=your_actual_api_key_here
```

⚠️ **مهم:** استبدل `your_actual_api_key_here` بالمفتاح الحقيقي!

---

### 3️⃣ تثبيت المكتبات

شغّل الأمر التالي:

```bash
npm install
```

هذا بيثبت `@google/generative-ai` تلقائياً.

---

### 4️⃣ تشغيل المشروع

```bash
npm start
```

---

### 5️⃣ نشر المشروع على Vercel

#### أ. إنشاء حساب Vercel:
1. اذهب إلى: https://vercel.com
2. سجّل الدخول بحساب GitHub

#### ب. رفع المشروع:
1. اضغط **"New Project"**
2. اختر المشروع من GitHub
3. في **Environment Variables**، أضف:
   - Key: `GEMINI_API_KEY`
   - Value: [المفتاح الحقيقي]

4. اضغط **"Deploy"**

---

## ✅ الآن الشات بوت جاهز!

- **محلياً:** http://localhost:3000
- **على Vercel:** سيعطيك رابط مثل `https://your-project.vercel.app`

---

## 🔧 استكشاف الأخطاء:

### المشكلة: الشات بوت يعطي نفس الرد دائماً
**الحل:** تأكد من:
- ملف `.env` موجود
- `GEMINI_API_KEY` صحيح
- شغّلت `npm install`

### المشكلة: Error 500 في API
**الحل:**
- تحقق من Console في المتصفح
- تأكد من API Key صالح
- تحقق من حد الاستخدام في Google AI Studio

---

## 📝 ملاحظات:

- **Gemini 1.5 Flash** مجاني لحد 15 طلب/دقيقة
- المفتاح يجب يبقى سري (لا ترفعه على GitHub!)
- ملف `.env` مضاف تلقائياً في `.gitignore`

---

**🎉 بالتوفيق!**
